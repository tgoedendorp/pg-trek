This folder contains the migration TypeScript files.

To create a new migration script, run from the following command from the command line:
deno run -- allow-all main.ts add <migration name>

If you want to manually add a TypeScript class implement interface IMigration, and it should work.
